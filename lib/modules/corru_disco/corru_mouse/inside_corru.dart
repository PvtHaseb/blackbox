import 'package:flutter/material.dart';

class InsideLib extends StatelessWidget {
  const InsideLib({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        
      ));
  }
}